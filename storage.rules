rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Project images: Public read, authenticated admin write
    match /project-images/{projectId}/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
